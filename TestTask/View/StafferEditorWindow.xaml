<Window x:Class="TestTask.View.StafferEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="280" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="ФИО:" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                 Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Должность:" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                  ItemsSource="{Binding Positions}" SelectedItem="{Binding Position}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата рождения:" VerticalAlignment="Center" Margin="0,0,0,8" />
        <DatePicker Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"
                    SelectedDate="{Binding Birth, ValidatesOnDataErrors=True}" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Grid.Column="0" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="15,5" Margin="0,0,8,0" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Padding="15,5" />
        </StackPanel>
    </Grid>
</Window>
