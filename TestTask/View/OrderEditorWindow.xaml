<Window x:Class="TestTask.View.OrderEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="320" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Сумма:" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                 Text="{Binding Sum, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата:" VerticalAlignment="Center" Margin="0,0,0,8" />
        <DatePicker Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                    SelectedDate="{Binding Date}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Сотрудник:" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"
                  ItemsSource="{Binding Staffers}" SelectedItem="{Binding SelectedStaffer, ValidatesOnDataErrors=True}"
                  DisplayMemberPath="FullName" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Контрагент:" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"
                  ItemsSource="{Binding CounterAgents}" SelectedItem="{Binding SelectedCounterAgent, ValidatesOnDataErrors=True}"
                  DisplayMemberPath="Name" />

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Grid.Column="0" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="15,5" Margin="0,0,8,0" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Padding="15,5" />
        </StackPanel>
    </Grid>
</Window>
